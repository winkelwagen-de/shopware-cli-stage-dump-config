# DRAFT — generated by bin/scan-plugin-migrations
# Review carefully before committing. Remove this header.
# Remove columns marked skip/review, resolve TODO comments, then run bin/cleanup-plugin-config.

plugin:
  name: "acris/cookie-consent"
  label: "EU Cookie Richtlinie Pro + automatischer Cookie Erkennung"

dump:
  tables:
    acris_cookie:


      rewrite:
        # TODO: review
        active: review
        # TODO: review
        default_value: review
        # TODO: review
        is_default: review
        # TODO: review
        provider: review
        # TODO: review
        script_position: review
        # TODO: review
        unknown: review
        # cookie_group_id: skip — structural column, remove before commit
        # cookie_id: skip — structural column, remove before commit
        # created_at: skip — structural column, remove before commit
        # id: skip — structural column, remove before commit
        # updated_at: skip — structural column, remove before commit
    acris_cookie_group:


      rewrite:
        # TODO: review
        identification: review
        # TODO: review
        is_default: review
        # created_at: skip — structural column, remove before commit
        # id: skip — structural column, remove before commit
        # updated_at: skip — structural column, remove before commit
    acris_cookie_group_translation:


      rewrite:
        # TODO: verify heuristic
        description: "'redacted'"
        # TODO: review
        title: review
        # acris_cookie_group_id: skip — structural column, remove before commit
        # created_at: skip — structural column, remove before commit
        # language_id: skip — structural column, remove before commit
        # updated_at: skip — structural column, remove before commit
    acris_cookie_sales_channel:


      rewrite:
        # cookie_id: skip — structural column, remove before commit
        # created_at: skip — structural column, remove before commit
        # sales_channel_id: skip — structural column, remove before commit
    acris_cookie_translation:


      rewrite:
        # TODO: verify heuristic
        description: "'redacted'"
        # TODO: review
        script: review
        # TODO: review
        title: review
        # acris_cookie_id: skip — structural column, remove before commit
        # created_at: skip — structural column, remove before commit
        # language_id: skip — structural column, remove before commit
        # updated_at: skip — structural column, remove before commit
